# Frontend config (NON-secret — safe to commit; Vite exposes VITE_* to the browser).
# The portal only ever holds the login JWT, never the Worker's secrets.
# Defaults to http://localhost:8787 in code if unset. Copy to .env to override.
VITE_API_BASE_URL=http://localhost:8787
