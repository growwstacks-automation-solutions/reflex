{
  "name": "@reflex/api",
  "private": true,
  "type": "module",
  "description": "Reflex AI plane — Cloudflare Worker that generates Upwork proposals via Claude (Haiku 4.5) with prompt caching.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": ">=0.30.0",
    "@neondatabase/serverless": ">=0.9.0",
    "bcryptjs": "^3.0.3",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": ">=4.20240909.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": ">=5.4.0",
    "wrangler": ">=3.60.0"
  }
}
