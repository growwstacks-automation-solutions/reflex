# macOS
.DS_Store

# Environment / secrets (never committed — see docs/RUNBOOK.md)
.env
.env.local
.env*.local

# Neon CLI local context (org/project id — not a secret, but machine-local)
.neon

# Dependencies / build output
node_modules/
dist/
build/

# Cloudflare Workers local dev/build cache
.wrangler/

# React/Vite
.next/
out/
next-env.d.ts
*.tsbuildinfo

#API Creds — both are gitignored: they have held real credentials.
apps/api/.dev.vars
apps/api/.dev.vars.example